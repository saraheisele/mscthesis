bash:
- audian -U 1.5 -c 0,8,15 eellogger1-2025030*.wav: open all files from that day as one and only channels 0, 8, 15
- audian -h: ?
- Navigate directories: cd <folder> (change directory, eisele/anyfolderinthere/..)
- cd ..: go one layer up/back
- List files: ls
- Create a folder: mkdir <folder>
- Move files: mv <source> <destination>
- Copy files: cp <source> <destination>
- Remove files: rm <file> (use with caution! This will permanently delete the file, there is no undo. For example, rm -rf / will delete the entire filesystem.)
- Edit files: Terminal editors like nano <file> or vim <file> allow you to create, edit, and save files from the terminal.
- sudo: super user do (admin rights ig)
- cat /etc/lsb-release: find info about version of OS
- apt is package manager that we use (for handling software packages): sudo apt update/upgrade/install/remove
- pwd: print working directory (shows current directory)
- <programm> --version: to check if a program/software is installed and get the current version
- touch <filename>: new file
- ls -ah: show all, human readable files in this directory
- esc, wq: to escape console text editor (e.g. vim)
- cat: concatenate (make several files into one file, create newfile, display files contents or specific lines of content)
- htop: shows kernels and what they do (whats going on on pc)
- ssh: secure shell; used to switch to user of different, remote pc
- su -l efish: switch user to efish without opening new shell (?)
- pyenv activate base: activate base for audian
- run .py file from terminal: python3 <filename>
- ctrl + r: reverse search for previous bash commands
- auf brasilien daten auf patricks pc zugreifen: sshfs efish@polarbear.am28.uni-tuebingen.de:/mnt/data1/efish /home/eisele/wrk/mscthesis/data/
- to call audian w thunderlab extension/function: call audianthunder (instead of audian)
- sudo apt update: adds everything that needs to be updated to list
- sudo apt upgrade: updates everything on that list


put downloaded/new eel files into file system:
1. navigate to eels-mfn2021/new
2. unzip /home/eisele/Downloads/<zip folder>
3. new folder in /new called recordings_<date>
4. ./cleandata <name of new folder>
5. rm -r /home/eisele/Downloads/<name of zip folder>

-> in case there are many files, not sorted after recording session: ./sort_blinks.sh <folder>

# check harddrive every few months:
# sudo smartctl -a /dev/sda | egrep "Reallocated|Pending|Uncorrectable|Power_On"
# sudo smartctl -t long /dev/sda

git:
- Initialize a Repository: git init (initialize new repo in current directory)
- Add Remote Repository (GitHub, Gittea): git remote add origin <repository-url> (connect initialized repo in cd with github repo; adds remote repo to local repo)
- Clone a Repository: git clone <repository-url> (Creates a local copy of an existing remote repository.)
- Check Status: git status
- Add Changes: git add <file-name>
- Add all changes: git add .
- Commit Changes: git commit -m "Commit message"
- Push to Remote Repository: git push origin main
- Pull from Remote Repository: git pull origin main
- Create a New Branch: git branch <branch-name>
- Switch Branches: git checkout <branch-name>
- Merge Branches: git merge <branch-name>
- View Commit History: git log
- Force pushing stuff: git push -f (don't use this! Only for rewriting existing repo)

shortcuts:
- alt + left arrow: go back a page
- ctrl + arrow: skip a word with cursor
- ctrl + alt + t: open terminal
- ctrl + tab: skip through tabs
- alt + tab: skip through windows
- ctrl + w: close tabs
- ctrl + q: clow windows
- ctrl + t/n: new tab
- ctrl + o: open stuff
- shift + arrows: highlight stuff
- ctrl + shift + arrows: highlight whole word (left/right) OR move current line (up/down)
- ctrl + shift + c/v: copy/paste from terminal
- ctrl + shift + 7: comment/uncomment several lines in vs code
- clear: make terminal empty
- ctrl + c: keyboard interrupt
- ctrl + h: show all hidden files (e.g. .git) in file explorer
- alt + backspace: delete whole last word before cursor

vscode:
- ctrl + I: get inline copilot
- ctrl + shift + up/down arrow: move line/current selection up or down
- alt + up/down arrow: cursor in multiple lines
- ctrl + 1: add comment on current line/selection
- ctrl + shift + 1: remove comment on current line/selection

- ctrl + enter: run current cell in interactive window
- ctrl + shift + enter: run all cells in interactive window
- alt + enter: run python file in terminal
- F5: run code in seperate terminal (not interactive)
- run script in terminal: cd to where script is saved, type: python3 <nameofscript.py>

- ctrl + J: toggle terminal
- ctrl + 0: focus on terminal
- ctrl + 2: focus on primary sidebar
- ctrl + 3: focus on first editor window
- ctrl + 4: focus on second editor window

- # %%: to make a cell
- select all cells, then shift + enter: run all cells/whole code in interactive/jupyter kernel so variables and packages are remembered
- shift + enter: run current cell (where cursor is)

- ctrl + shirt + M: Problems window
- ctrl + shift + P: Command Pallette (e.g. to access JSON files)
- ctrl + ,: Settings

- pdb.set_trace()/IPython.embed(): use this in code to open interactive debug window at this line (pdb is from python debugger extension)
- ctrl + shift + D: Debug console (built in vs code GUI, works with breakpoints, start w. F5)
- run and Debug panel (left sidebar): this is where the debug process in the debug console is controlled (settings, start/end,...)

How to get structure of patricks h5 files:
- cd to ~/wrk/eeltracker
- python3 print_nix_layout.py ~/wrk/mscthesis/data/newdata/eels-mfn2021_dummy_pulses_redetected/berlin_tank_site/recordings_2024-02-13_pulses.h5
OR: section.pprint()
